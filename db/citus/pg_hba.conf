# PostgreSQL Client Authentication Configuration File for Citus nodes
# TYPE  DATABASE   USER        ADDRESS       METHOD
local   all        all                       trust
host    all        all         127.0.0.1/32  trust
host    all        all         ::1/128       trust

# Citus inter-node: coordinator <-> workers (внутри Docker-сети — безопасно)
host    all        citus_user  0.0.0.0/0     trust

# Slave PostgreSQL Configuration for Streaming Replication

listen_addresses = '*'
port = 5432

# Replication settings
wal_level = replica
hot_standby = on
hot_standby_feedback = on

# Resource Consumption
max_connections = 100
